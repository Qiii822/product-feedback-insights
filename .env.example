# 运行环境
ENVIRONMENT=development
LOG_LEVEL=INFO

# 数据库（默认 SQLite，零配置；后续可改为 Postgres 连接串）
DATABASE_URL=sqlite:///./data/app.db

# LLM Provider（当前为 mock；未来接入 DeepSeek）
LLM_PROVIDER=mock
LLM_MODEL=fake
DEEPSEEK_API_KEY=

# Tracing（当前 memory；后续可替换为 langsmith 等）
TRACER_BACKEND=memory
